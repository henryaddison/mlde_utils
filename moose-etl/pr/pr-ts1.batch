#! /usr/bin/bash

set -euxo pipefail

var=pr

# for year in $(seq 1981 1 2000); do
#   mlde preprocess create-variable --variable ${var} --year ${year} --resolution 2.2km
# done

for year in $(seq 1981 1 2000); do
  for sf in 4 8 16 27 32; do
    mlde preprocess create-variable --variable ${var} --year ${year} --resolution 2.2km-coarsened-${sf}x
  done
done
